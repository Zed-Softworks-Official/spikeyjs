{
  "name": "spikeyjs",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm run spikey:build && pnpm run cli:build",
    "spikey:build": "cd packages/spikey && pnpm run build",
    "cli:build": "cd packages/cli && pnpm run build"
  },
  "devDependencies": {
    "@eslint/js": "^9.16.0",
    "@types/node": "^22.10.1",
    "eslint": "^9.16.0",
    "globals": "^15.13.0",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.17.0"
  },
  "packageManager": "pnpm@9.14.0",
  "engines": {
    "node": ">=20.10.0",
    "pnpm": ">=9.14.0"
  }
}
